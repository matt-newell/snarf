// import {  test } from '@oclif/test';
// import {  TestSession } from '@salesforce/cli-plugins-testkit';
// import { SnowflakeImportResult } from '../../../src/commands/types';

describe('Snarf testing snarf', async () => {
  // const testSession = await TestSession.create({
  //   project: {
  //     name: 'Snarf-Plugin',
  //   },
  //   scratchOrgs: [
  //     { executable: 'sfdx', setDefault: true }
  //   ]
  // });
  // const username = [...testSession.orgs.keys()][0];

  // test
  //   .stdout()
  //   .command(['snowflake', '--help'])
  //   .it('runs snowflake --help', (ctx) => {
  //     console.log(ctx);
  //     // expect(ctx.stdout).to.contain('Commands to import data from Snowflake');
  //   });
  
  // test
  //   .stdout()
  //   .command(['snowflake:import', '--help', '-o', username])
  //   .it('runs snowflake --help', (ctx) => {
  //     console.log(ctx);
      // expect(ctx.stdout).to.contain('Commands to import data from Snowflake');
    // });

  // test
  //   .stdout()
  //   .command(['snowflake:import', '--json'])
  //   .it('runs snowflake import with --json and no provided name', (ctx) => {
  //     const { result } = JSON.parse(ctx.stdout) as { result: SnowflakeImportResult };
  //     expect(result.account).to.equal('Account');
  //   });

  // test
  //   .stdout()
  //   .command(['snowflake import', '-a', 'Account', '--username', 'mattnewell', '-m', 'Upsert', '--target-org', 'org', '--json'])
  //   .it('runs snowflake import --name Astro --json', (ctx) => {
  //     const { result } = JSON.parse(ctx.stdout) as { result: SnowflakeImportResult };
  //     expect(result.account).to.equal('Account');
  //     expect(result.username).to.equal('mattnewell');
  //     expect(result.method).to.equal('Upsert');
      
  //   });
});
